<?php

the_content();

  $section_meta = get_post_meta( get_the_ID(), 'section_meta_options', true );
 

 if (is_array($an_array) && isset($an_array['type'])) {
    $gd= $section_meta['']
 
 }
   

?>